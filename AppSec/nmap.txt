https://www.stationx.net/nmap-cheat-sheet/

	Download and install:
sudo apt-get install nmap

	Check version if was preisntalled
nmap --version

	Help
nmap -h

	Scan IP:
sudo nmap -T4 -v -O -p- 159.223.230.244 -oN /home/nmap_out

-If some services are found try to find if there are some exploits about that service using Exploit postgres(check linuxCommands file)
